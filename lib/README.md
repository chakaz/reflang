This directory contains tests for the library exposed under lib/. It does not
include the usual set of files (src, gen, test).

Unless you modify the library you probably shouldn't care about this dir.
